import 'package:flutter/material.dart';

class CustomCategoryC extends StatelessWidget {
  const CustomCategoryC({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}